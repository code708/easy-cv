---
const { thickness } = Astro.props;
const color = Astro.props.color?.startsWith('--')
	? `var(${Astro.props.color})`
	: Astro.props.color;
---

<div
	class={`my-8 w-full clear-left`}
	style=`height: ${thickness}px; background-color: ${color}`
>
</div>
